# プロジェクトについて

- サイドパネルのコンソール等がある場所にボタン一つ追加して、コンソールのように新しい領域を出す。
- その領域にワークスペースのようにタブ構造を追加する。
- タブの種類は以下。
    - Checkpoint
    - Loras
    - Prompts
    - Images
    - 
## 以下は現在調査項目
### 以下項目を調査して、設計書に反映してください。
- Loraマネージャを解析して、CheckpointとLorasのタブを実装する。
- Promptsタブでは、テキスト編集等を行い、それをYamlファイルとして保存できるようにする。
- Imagesタブでは、ドラッグドロップされたものを表示できるようにする。
- 上記のタブを実装するにあたり、例えばチェックポイント等のデータを持ったノードを作成できるか。
  - 例1：LorasのタブでLoraの追加等が行われていた場合は、それを通常のチェックポイントから繋いだLoraローダーと同じ働きをするようにする。
- 既存のノードを拡張機能から使うことはできるのか。
- インストールされた拡張機能は、別の拡張機能からアクセス可能か。